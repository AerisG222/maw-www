@page
@model MawPageModel
@{
    Layout = "_Layout";
    Model.Title = "Tools";
    Model.Area = PrimaryNavArea.Tools;
    Model.SetHtmlPageTitle(PrimaryNavArea.Tools);
}

@await Html.PartialAsync("_PageMenuList", ToolsMenu.Menus)

@page
@using MawWww.Extensions
@model MawPageModel
@{
    Model.Title = "Access Denied";
    Model.Area = PrimaryNavArea.Account;
    Model.SetHtmlPageTitle(PrimaryNavArea.Account, "Access Denied");

    var alert = new Alert(AlertStatus.Error, "Sorry, you do not have access to the requested resource.");
}

@await Html.PartialAsync("_Alert", alert)

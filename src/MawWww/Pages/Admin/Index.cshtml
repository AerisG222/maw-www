@page
@model MawPageModel
@{
    Model.Title = "Admin";
    Model.Area = PrimaryNavArea.Admin;
    Model.SetHtmlPageTitle(PrimaryNavArea.Admin);
}

@section sidebar {
    <partial name="_AdminSidebar" />
}

ADMIN!

@if(User.Identity.IsAuthenticated)
{
    foreach(var claim in User.Claims)
    {
        <p>@claim.Type: @claim.Value</p>
    }
}

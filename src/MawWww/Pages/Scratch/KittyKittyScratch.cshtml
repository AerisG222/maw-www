@page
@model MawPageModel
@{
    Model.Title = "Kitty Kitty Scratch";
    Model.Area = PrimaryNavArea.Scratch;
    Model.SetHtmlPageTitle(PrimaryNavArea.Scratch, "Kitty Kitty Scratch");
}

@section sidebar {
    <partial name="_ScratchSidebar" />
}

<p>
    Move along, nothing to see here!
</p>

@await Html.PartialAsync("_IFrame", new IFrame("/scratch/kitty-kitty-scratch.html", 480))
